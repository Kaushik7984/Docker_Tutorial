# Frontend - MERN Docker App

## Setup

1. Install dependencies:
   ```sh
   npm install
   ```

2. Start the development server:
   ```sh
   npm start
   ```

Ensure the backend is running before accessing the frontend.